---
import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, "");

const services = [
  {
    icon: `${baseUrl}/icons/calefaccion.webp`,
    key: "calefaccion",
  },
  {
    icon: `${baseUrl}/icons/estacionamiento.webp`,
    key: "parking",
  },
  {
    icon: `${baseUrl}/icons/cocina.webp`,
    key: "cocina",
  },
  {
    icon: `${baseUrl}/icons/cama.webp`,
    key: "ropa",
  },
  {
    icon: `${baseUrl}/icons/caja-fuerte.webp`,
    key: "caja",
  },
  {
    icon: `${baseUrl}/icons/wifi.webp`,
    key: "wifi",
  },
  {
    icon: `${baseUrl}/icons/cuna.webp`,
    key: "cuna",
  },
  {
    icon: `${baseUrl}/icons/mucama.webp`,
    key: "mucama",
  },
  {
    icon: `${baseUrl}/icons/amenities.webp`,
    key: "amenities",
  },
];
---

<section class="services reveal" id="servicios">
  <div class="container">
    <div class="section-header">
      <span class="section-tag">{t("services.tag")}</span>
      <h2>{t("services.title")}</h2>
      <p>{t("services.subtitle")}</p>
    </div>
    <div class="services-grid">
      {
        services.map((s) => (
          <div class="service-card glass-card">
            <div class="service-icon">
              {s.icon.startsWith("<svg") ? (
                <Fragment set:html={s.icon} />
              ) : (
                <img src={s.icon} alt={t(`services.${s.key}_title` as any)} />
              )}
            </div>
            <h3>{t(`services.${s.key}_title` as any)}</h3>
            <p>{t(`services.${s.key}_desc` as any)}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .services {
    padding: 6rem 0;
    background: transparent;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-tag {
    display: inline-block;
    color: var(--white);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 0.75rem;
  }

  .section-header h2 {
    font-size: clamp(1.8rem, 3.5vw, 2.75rem);
    font-weight: 800;
    color: var(--title-color);
    margin-bottom: 0.75rem;
  }

  .section-header p {
    color: var(--text-muted);
    font-size: 1.05rem;
    max-width: 500px;
    margin: 0 auto;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2.5rem;
  }

  .service-card {
    padding: 3.5rem 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 2rem;
    transition: var(--transition);
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .service-card:hover {
    transform: translateY(-8px);
    border-color: var(--soft-apricot);
  }

  .service-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--soft-apricot);
    transition: var(--transition);
  }

  .service-icon svg,
  .service-icon img {
    width: 140px;
    height: 140px;
    object-fit: contain;
    /* White color for all icons */
    filter: brightness(0) invert(1);
    opacity: 0.9;
    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .service-card:hover .service-icon img,
  .service-card:hover .service-icon svg {
    transform: scale(1.1);
    opacity: 1;
  }

  .service-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 0.5rem;
    display: grid;
    text-align: center;
  }

  .service-card p {
    font-size: 1rem;
    color: var(--text-main);
    line-height: 1.6;
    display: grid;
    text-align: center;
    opacity: 0.8;
  }

  @media (max-width: 768px) {
    .services-grid {
      grid-template-columns: 1fr 1fr;
      gap: 1.5rem;
    }

    .service-icon svg,
    .service-icon img {
      width: 100px;
      height: 100px;
    }

    .service-card {
      padding: 2.5rem 1rem;
    }
  }

  @media (max-width: 480px) {
    .services-grid {
      grid-template-columns: 1fr;
    }

    .service-icon svg,
    .service-icon img {
      width: 120px;
      height: 120px;
    }
  }
</style>
