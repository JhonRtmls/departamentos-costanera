---
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<html>
  <head>
    <meta charset="utf-8" />
    <title>Departamentos Costanera — Redirigiendo...</title>
    <link rel="canonical" href={`${base}/es/`} />
    <script is:inline>
      const supported = ["es", "en", "zh"];
      const browserLang = (
        navigator.language ||
        navigator.languages?.[0] ||
        "en"
      ).toLowerCase().split("-")[0];
      const locale = supported.includes(browserLang) ? browserLang : "en";
      const path = window.location.pathname.replace(/\/+$/, "");
      window.location.replace(path + "/" + locale + "/");
    </script>
    <noscript>
      <meta http-equiv="refresh" content="0;url=./es/" />
    </noscript>
  </head>
  <body>
    <p>Redirigiendo a <a href="./es/">Español</a> | <a href="./en/">English</a> | <a href="./zh/">中文</a></p>
  </body>
</html>
